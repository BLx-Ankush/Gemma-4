# AEGIS Python dependencies
# Install in Termux: pip install -r requirements.txt
# All packages are Termux/AArch64-compatible.
# faster-whisper and pyttsx3 install cleanly via pip without compilation.
# llama-cpp-python is NOT listed — we use a pre-compiled llama-server binary instead.

# ── Core inference + routing ──────────────────────────────────────────────────
requests==2.32.3

# ── Web server ────────────────────────────────────────────────────────────────
flask==3.1.0
flask-cors==5.0.0

# ── Voice pipeline ────────────────────────────────────────────────────────────
faster-whisper==1.1.0
pyttsx3==2.98

# ── System utilities ──────────────────────────────────────────────────────────
psutil==6.1.0          # RAM detection (optional — falls back to /proc/meminfo)
numpy==2.2.0           # required by faster-whisper
Pillow==11.1.0         # image handling for VEDA vision path
tqdm==4.67.1           # progress bars in setup scripts

# ── Testing + benchmark ───────────────────────────────────────────────────────
pytest==8.3.4
